package src.classeA;

public class ValorPadrao {
	/*
	 * byte, short, int, long o valor padrão será 0
	 * float, double o valor padrão é 0.0
	 * boolean é false char '\u0000' primeiro valor da tabela unicode
	 * Objetos -> null quer dizer que não aponta para nenhum local de memória
	 * uma variável local não é iniciada por padrão, e não é possível usar a mesma até atribuir o valor (inicializar a mesma)
	 * null != de vazio
	 * uma constante "final type name;" também não recebe um valor padrão
	 * a inicialização da final precisa ser feita até a criação do objeto
	 * podendo ser feita diretamente na var ou pelo metodo construtor
	 */
}
